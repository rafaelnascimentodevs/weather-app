/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'openweathermap.org', 'images7.alphacoders.com', 'icomoon.io'],
    },
};

export default nextConfig;
